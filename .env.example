# Development Environment - SQLite Local
DATABASE_URL="file:./prisma/dev.db"
NODE_ENV=development
PORT=5000
TZ=Europe/Paris

# On Railway, DATABASE_URL is automatically injected by PostgreSQL service
# Example (do not commit actual PostgreSQL URLs):
# DATABASE_URL="postgresql://user:password@host:5432/dbname"
